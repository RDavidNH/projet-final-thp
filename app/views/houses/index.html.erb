<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h1>Houses</h1>
        </div>
    </div>
    <div class="row">
        <!--
        <div class="col-sm-12">
            <%= link_to 'New house', new_house_path %>
            <br>

            <% @houses.each do |house| %>
            <%=  link_to house.title, house_path(house.id) %>
            <%=  link_to 'Modifier', edit_house_path(house.id) %>

            <br>
            <% end %>
        </div>
        -->
        <% @houses.each do |house| %>

    <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12">
        <a href="<%= house_path(house.id) %>">
        <div class="box box--pricing" data-scroll-reveal="enter bottom and move 10px">
            <div class="box__wrapper shadow-sm">
                <div class="box__image **height-300px">
                    <div class="**img-into-bg">
                        <%= image_tag 'img1', style: 'width:100%' %>
                    </div>
                </div>
                <div class="box__header pt-3">
                    <h4><%= house.title %></h4>
                    <h4 class="price"><%= '140.000' %> Ar</h4>
                </div>
                <div class="box__content p-0">
                    <h5 class="pt-3 text-align-left pl-5 pr-5">A louer</h5>
                    <!-- <ul class="list-unstyled">
                        <li>245m<sup>2</sup></li>
                        <li>1 Bathroom</li>
                        <li>10 m<sup>2</sup> Balcony</li>
                    </ul> -->
                    <p class="text-align-left pl-5 pr-5 m-0"><%= house.description %></p>
                </div>
                <!--
                <div class="box__footer">
                    <%= link_to 'Voir', house_path(house.id), class: 'btn btn-primary' %>
                </div>
                -->
            </div>
            <!--end box__wrapper-->
        </div>
        <!--end box-pricing-->
        </a>
    </div>
        <% end %>

    </div>
</div>
